# 🚀 Быстрый старт - Vercel с одобренным API

## ✅ Ваша заявка одобрена! Что дальше?

Теперь когда ваша заявка на Yandex Direct API одобрена, нужно сделать всего **3 простых шага**:

---

## 📋 Шаг 1: Проверьте переменные окружения в Vercel

1. Откройте ваш проект на Vercel: https://vercel.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Убедитесь что добавлены обе переменные:

| Переменная | Значение | Должно быть |
|------------|----------|-------------|
| `YANDEX_DIRECT_TOKEN` | `y0__xCp7sHEAxjO8jsgo6bhshWsrA5ZdgQApJVAbUcp2p-dluCANQ` | ✅ Все окружения (Production, Preview, Development) |
| `YANDEX_DIRECT_LOGIN` | `getuniq-u78912-1` | ✅ Все окружения (Production, Preview, Development) |

### Если переменных нет:

Добавьте их:
- Нажмите **"Add New"**
- **Key:** `YANDEX_DIRECT_TOKEN`
- **Value:** `y0__xCp7sHEAxjO8jsgo6bhshWsrA5ZdgQApJVAbUcp2p-dluCANQ`
- **Environments:** Выберите все (Production, Preview, Development)
- Нажмите **"Save"**

Повторите для `YANDEX_DIRECT_LOGIN`.

---

## 📋 Шаг 2: Redeploy проекта

После добавления переменных окружения (или обновления кода):

### Вариант A: Автоматический redeploy (рекомендуется)

```bash
# Смерджите изменения в main
git checkout main
git merge claude/setup-api-integration-017JvQeHaHbsMMCH7ZBeeUC2
git push origin main
```

Vercel автоматически задеплоит обновления!

### Вариант B: Ручной redeploy через Dashboard

1. Откройте Vercel Dashboard
2. Перейдите в **Deployments**
3. Нажмите на последний деплой
4. Нажмите **"⋮"** (три точки) → **"Redeploy"**
5. Подтвердите

---

## 📋 Шаг 3: Протестируйте работу

### 3.1 Проверьте API endpoints

Откройте в браузере (замените `your-project` на название вашего проекта):

**Статистика:**
```
https://your-project.vercel.app/api/stats
```

**Должны увидеть JSON с данными:**
```json
{
  "success": true,
  "stats": {
    "total_impressions": 12345,
    "total_clicks": 456,
    "total_cost": 7890.50,
    ...
  }
}
```

**Кампании:**
```
https://your-project.vercel.app/api/campaigns
```

**Должны увидеть:**
```json
{
  "success": true,
  "campaigns": [
    {
      "Id": 123456,
      "Name": "Ваша кампания",
      "Status": "ACCEPTED",
      ...
    }
  ]
}
```

### 3.2 Проверьте Dashboard

1. Откройте ваш сайт: `https://your-project.vercel.app`
2. Перейдите в раздел **"Яндекс.Директ"**
3. Подождите 2-3 секунды
4. **Должны загрузиться реальные данные!** 🎉

Откройте консоль браузера (F12) - должно быть:
```
✅ Данные загружены из Yandex Direct API (Vercel)
```

---

## 🔧 Если что-то не работает

### Ошибка: "Missing credentials"

**Решение:** Переменные окружения не добавлены или не применились.

1. Проверьте что переменные добавлены в Vercel
2. Убедитесь что выбраны **все окружения**
3. **Обязательно сделайте Redeploy после добавления переменных!**

### Ошибка: "Yandex API error: 401" или "Unauthorized"

**Решение:** Токен неправильный или истёк.

1. Проверьте что токен скопирован полностью (весь)
2. Убедитесь что нет лишних пробелов
3. Если токен старый - получите новый через OAuth

### Показывает демо-данные

**Решение:** API не отвечает или возвращает ошибку.

1. Откройте API endpoint в браузере (`/api/stats`)
2. Посмотрите что возвращается
3. Проверьте логи в Vercel:
   - Deployments → ваш деплой → Functions → `/api/stats` → Logs

### API медленно работает (холодный старт)

**Это нормально!** На бесплатном плане Vercel:
- Первый запрос: 2-5 секунд (cold start)
- Последующие: < 1 секунда

Решение для production: обновите на платный план ($20/мес).

---

## 📊 Что у вас теперь есть

После успешной настройки:

✅ **Реальные данные из Яндекс.Директ**
- Показы, клики, CTR, стоимость
- Список кампаний
- Статистика за последние 30 дней

✅ **Автоматическое обновление**
- Каждый `git push` → автоматический деплой
- Данные обновляются при каждом обновлении страницы

✅ **Безопасность**
- Токены хранятся в переменных окружения
- HTTPS из коробки
- CORS настроен правильно

---

## 🎯 Следующие шаги (опционально)

### 1. Добавьте кастомный домен

Вместо `.vercel.app` используйте свой домен:
- Settings → Domains → Add
- Следуйте инструкциям

### 2. Настройте мониторинг

- Vercel Analytics - отслеживание посещений
- Uptime monitoring - проверка доступности

### 3. Расширьте функционал

- Добавьте графики истории за 90 дней
- Настройте уведомления при достижении бюджета
- Интегрируйте другие рекламные платформы

---

## 🆘 Проблемы?

### Посмотрите логи:

1. **Frontend логи:**
   - Откройте сайт
   - F12 → Console
   - Посмотрите на сообщения

2. **Backend логи:**
   - Vercel Dashboard
   - Deployments → последний деплой
   - Functions → `/api/stats` или `/api/campaigns`
   - Logs

### Частые проблемы:

| Проблема | Решение |
|----------|---------|
| "Missing credentials" | Добавьте переменные окружения + Redeploy |
| "401 Unauthorized" | Проверьте токен (может истёк или неправильный) |
| Показывает mock данные | Проверьте что API endpoints работают |
| Медленно загружается | Нормально для бесплатного плана (cold start) |

---

## ✅ Чеклист готовности

Проверьте что всё сделано:

- [ ] Переменные окружения добавлены в Vercel (`YANDEX_DIRECT_TOKEN`, `YANDEX_DIRECT_LOGIN`)
- [ ] Выбраны **все окружения** для переменных
- [ ] Сделан Redeploy после добавления переменных
- [ ] `/api/stats` возвращает реальные данные (не ошибку)
- [ ] `/api/campaigns` возвращает список кампаний
- [ ] Dashboard показывает реальные данные (не mock)
- [ ] В консоли браузера: "✅ Данные загружены из Yandex Direct API"

---

## 🔗 Полезные ссылки

- **Ваш Vercel Dashboard:** https://vercel.com/dashboard
- **Yandex Direct API Docs:** https://yandex.ru/dev/direct/doc/
- **OAuth токены:** https://oauth.yandex.ru/

---

## 📝 Что было исправлено

В этом обновлении:
- ✅ Убрали поле `Statistics` из запроса кампаний (вызывало ошибки)
- ✅ Обновили frontend для работы с обоими форматами API
- ✅ Добавили полную поддержку Vercel serverless functions

Теперь API работает корректно с одобренной заявкой! 🎉

---

**Готово! Просто сделайте Redeploy и наслаждайтесь реальными данными! 🚀**
