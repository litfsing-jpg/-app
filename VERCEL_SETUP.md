# 🚀 Деплой на Vercel с Yandex Direct API

## ✅ Что уже готово

У вас полностью настроен проект для деплоя на Vercel:

### 📁 Структура проекта:

```
/
├── api/                          # Serverless Functions (Python)
│   ├── stats.py                  # GET /api/stats
│   └── campaigns.py              # GET /api/campaigns
├── demo/                         # Frontend
│   ├── js/yandex-direct.js       # Автоматически работает с API
│   └── css/styles.css
├── index.html                    # Главная страница
├── vercel.json                   # Конфигурация Vercel
├── requirements.txt              # Python зависимости
└── .env.example                  # Шаблон переменных окружения
```

### ✨ Особенности:

- ✅ **Serverless Python Functions** - API работает автоматически
- ✅ **Автоматическое определение окружения** - frontend сам переключается между local/production
- ✅ **CORS настроен** - работает из браузера
- ✅ **Переменные окружения** - токены защищены

---

## 📋 Шаг 1: Настройка переменных окружения в Vercel

### Через Web Interface:

1. Откройте ваш проект на Vercel: https://vercel.com/dashboard
2. Выберите проект (или создайте новый)
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

| Имя переменной | Значение | Окружение |
|----------------|----------|-----------|
| `YANDEX_DIRECT_TOKEN` | `y0__xCp7sHEAxjO8jsgo6bhshWsrA5ZdgQApJVAbUcp2p-dluCANQ` | Production, Preview, Development |
| `YANDEX_DIRECT_LOGIN` | `getuniq-u78912-1` | Production, Preview, Development |

**Важно:** Выберите **все окружения** (Production, Preview, Development)

### Через Vercel CLI:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в аккаунт
vercel login

# Добавьте переменные
vercel env add YANDEX_DIRECT_TOKEN
# Вставьте: y0__xCp7sHEAxjO8jsgo6bhshWsrA5ZdgQApJVAbUcp2p-dluCANQ

vercel env add YANDEX_DIRECT_LOGIN
# Вставьте: getuniq-u78912-1
```

---

## 📋 Шаг 2: Деплой проекта

### Вариант 1: Через Web Interface (рекомендуется для начинающих)

1. **Подключите GitHub репозиторий:**
   - Откройте https://vercel.com/new
   - Выберите **Import Git Repository**
   - Подключите ваш репозиторий GitHub

2. **Настройте проект:**
   - **Framework Preset:** Other
   - **Root Directory:** `./` (оставьте по умолчанию)
   - **Build Command:** (оставьте пустым)
   - **Output Directory:** (оставьте пустым)

3. **Нажмите Deploy** 🚀

Vercel автоматически:
- Обнаружит Python функции в папке `/api`
- Установит зависимости из `requirements.txt`
- Задеплоит frontend
- Настроит маршруты из `vercel.json`

### Вариант 2: Через Vercel CLI

```bash
# В корне проекта выполните
vercel

# Следуйте инструкциям:
# ? Set up and deploy "~/your-project"? [Y/n] y
# ? Which scope do you want to deploy to? Your Name
# ? Link to existing project? [y/N] n
# ? What's your project's name? yandex-direct-dashboard
# ? In which directory is your code located? ./

# Деплой завершён! Получите URL проекта
```

### Вариант 3: Автоматический деплой через GitHub

Vercel автоматически деплоит при каждом push:

```bash
git add .
git commit -m "Setup Yandex Direct API integration"
git push origin main
```

Vercel автоматически создаст новый деплой.

---

## 📋 Шаг 3: Проверка работы

### После деплоя:

1. **Откройте ваш сайт:**
   ```
   https://your-project.vercel.app
   ```

2. **Проверьте API endpoints:**

   **Статистика:**
   ```
   https://your-project.vercel.app/api/stats
   ```

   **Кампании:**
   ```
   https://your-project.vercel.app/api/campaigns
   ```

3. **Откройте Dashboard:**
   - Перейдите в раздел **"Яндекс.Директ"**
   - Данные должны загрузиться из реального API
   - Проверьте консоль браузера (F12) - должно быть сообщение:
     ```
     ✅ Данные загружены из Yandex Direct API (Vercel)
     ```

---

## 🔧 Troubleshooting

### 1. API endpoints возвращают 500 ошибку

**Причина:** Не настроены переменные окружения.

**Решение:**
- Проверьте что `YANDEX_DIRECT_TOKEN` и `YANDEX_DIRECT_LOGIN` добавлены в Vercel
- Перезапустите деплой после добавления переменных

### 2. Данные не загружаются

**Причина:** Frontend использует демо-данные (fallback).

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте сетевые запросы (вкладка Network)
3. Найдите запросы к `/api/stats` и `/api/campaigns`
4. Посмотрите ответ - должен быть статус 200

### 3. CORS ошибки

**Причина:** CORS уже настроен в коде, но может быть кэш.

**Решение:**
- Очистите кэш браузера
- Откройте в режиме инкогнито
- Проверьте что serverless функции содержат CORS headers

### 4. Python функции не работают

**Причина:** Vercel не установил зависимости.

**Решение:**
- Проверьте что `requirements.txt` в корне проекта
- Перезапустите деплой через Vercel Dashboard
- Проверьте логи деплоя на наличие ошибок

---

## 📊 Мониторинг и логи

### Просмотр логов:

1. **Через Web Interface:**
   - Откройте проект на Vercel
   - Перейдите в **Deployments**
   - Выберите последний деплой
   - Нажмите **View Function Logs**

2. **Через CLI:**
   ```bash
   vercel logs
   ```

### Полезные логи:

- **Build logs** - ошибки при деплое
- **Function logs** - ошибки в Python функциях
- **Edge Network logs** - запросы к API

---

## 🎯 Оптимизация для Production

### 1. Кэширование ответов API

Добавьте в `vercel.json`:

```json
{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "s-maxage=300, stale-while-revalidate"
        }
      ]
    }
  ]
}
```

### 2. Регионы деплоя

По умолчанию Vercel деплоит в ближайший регион. Для России:
- Functions будут в ближайшем к пользователю edge location
- Можно указать конкретный регион в настройках проекта

### 3. Мониторинг использования

- Vercel предоставляет 100 GB bandwidth бесплатно
- Yandex Direct API имеет свои лимиты
- Рекомендуется добавить кэширование на стороне клиента

---

## 🔐 Безопасность на Production

### ✅ Уже реализовано:

- ✅ Токены в переменных окружения (не в коде)
- ✅ `.env` добавлен в `.gitignore`
- ✅ CORS настроен правильно
- ✅ HTTPS автоматически (Vercel)

### 📝 Рекомендации:

1. **Регулярно обновляйте токены:**
   - Получите новый токен через https://oauth.yandex.ru
   - Обновите в Vercel Environment Variables

2. **Мониторинг доступа:**
   - Проверяйте логи на подозрительную активность
   - Используйте Vercel Analytics

3. **Ограничение CORS (опционально):**
   Если хотите ограничить доступ только с вашего домена, обновите в `api/stats.py` и `api/campaigns.py`:
   ```python
   self.send_header("Access-Control-Allow-Origin", "https://your-domain.vercel.app")
   ```

---

## 🚀 Следующие шаги

### Улучшения для рассмотрения:

1. **Добавить новые endpoints:**
   - `/api/report` - детальные отчёты
   - `/api/adgroups` - группы объявлений
   - `/api/export` - экспорт в CSV

2. **Webhooks:**
   - Уведомления при изменении кампаний
   - Автоматические отчёты

3. **Dashboard улучшения:**
   - Графики в реальном времени
   - Сравнение периодов
   - Прогнозы на основе AI

---

## 📚 Полезные ссылки

- **Vercel документация:** https://vercel.com/docs
- **Vercel Python Runtime:** https://vercel.com/docs/runtimes#official-runtimes/python
- **Yandex Direct API:** https://yandex.ru/dev/direct/doc/
- **Ваш проект на GitHub:** https://github.com/litfsing-jpg/-app

---

## ✅ Чеклист готовности к деплою

- [x] Serverless функции созданы (`/api/stats.py`, `/api/campaigns.py`)
- [x] `vercel.json` настроен
- [x] Frontend обновлён для работы с Vercel
- [x] `requirements.txt` содержит все зависимости
- [ ] Переменные окружения добавлены в Vercel
- [ ] Проект задеплоен на Vercel
- [ ] API endpoints работают
- [ ] Dashboard показывает реальные данные

---

## 💡 Полезные команды

```bash
# Локальная разработка с Vercel Dev
vercel dev

# Деплой на production
vercel --prod

# Просмотр логов
vercel logs

# Список всех деплоев
vercel ls

# Удалить старые деплои
vercel rm deployment-url
```

---

**Статус:** ✅ Проект готов к деплою на Vercel
**Дата:** 2025-11-28
**Ветка:** `claude/setup-api-integration-017JvQeHaHbsMMCH7ZBeeUC2`

🚀 **Задеплойте сейчас и получите работающее приложение через 2 минуты!**
