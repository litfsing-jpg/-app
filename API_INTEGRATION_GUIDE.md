# 🚀 Руководство по запуску API интеграции Яндекс.Директ

## ✅ Что было настроено

Полная интеграция с Яндекс.Директ API готова к запуску:

### Созданные файлы:

1. **`.env`** - Конфигурация с токеном и логином
2. **`yandex_backend.py`** - FastAPI сервер для работы с Яндекс.Директ API
3. **`requirements.txt`** - Обновлён с необходимыми зависимостями
4. **`demo/js/yandex-direct.js`** - Frontend обновлён для подключения к реальному API

---

## 📋 Шаг 1: Проверка учётных данных

Ваши текущие данные для API (уже настроены в `.env`):

```bash
YANDEX_DIRECT_TOKEN=y0__xCp7sHEAxjO8jsgo6bhshWsrA5ZdgQApJVAbUcp2p-dluCANQ
YANDEX_DIRECT_LOGIN=getuniq-u78912-1
```

**Важно:** Файл `.env` уже создан и содержит ваши данные.

---

## 📋 Шаг 2: Установка зависимостей

Если вы работаете локально, установите зависимости:

```bash
# Убедитесь что у вас Python 3.7+
python3 --version

# Установите зависимости
pip install -r requirements.txt
```

Зависимости уже установлены в текущей среде:
- ✅ requests==2.31.0
- ✅ fastapi==0.104.1
- ✅ uvicorn[standard]==0.24.0
- ✅ python-dotenv==1.0.0

---

## 📋 Шаг 3: Запуск Backend сервера

### Локальный запуск:

```bash
python yandex_backend.py
```

Сервер запустится на `http://localhost:8000`

### Проверка работы:

Откройте в браузере:
- **Главная страница:** http://localhost:8000
- **API документация:** http://localhost:8000/docs
- **Статистика:** http://localhost:8000/api/yandex-direct/stats
- **Кампании:** http://localhost:8000/api/yandex-direct/campaigns

### Доступные API endpoints:

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Health check |
| `/api/yandex-direct/stats` | GET | Общая статистика за 30 дней |
| `/api/yandex-direct/campaigns` | GET | Список всех кампаний |
| `/api/yandex-direct/campaigns/{id}` | GET | Данные конкретной кампании |
| `/api/yandex-direct/report?days=30` | GET | Отчёт за N дней |
| `/api/yandex-direct/export` | GET | Экспорт в CSV |
| `/api/yandex-direct/adgroups` | GET | Группы объявлений |

---

## 📋 Шаг 4: Запуск Frontend

### Локальная разработка:

Откройте `index.html` в браузере или запустите локальный сервер:

```bash
# С помощью Python
python -m http.server 3000

# Или с помощью Node.js
npx serve .
```

Frontend автоматически подключится к backend на `http://localhost:8000`

### Проверка интеграции:

1. Откройте Dashboard
2. Перейдите в раздел **"Яндекс.Директ"**
3. Данные должны загрузиться из реального API

Если API недоступен, Dashboard автоматически использует демо-данные.

---

## 🔧 Troubleshooting

### Ошибка: "Unable to connect to proxy: 403 Forbidden"

**Причина:** Сетевые ограничения в текущей среде разработки.

**Решение:**
- Запустите backend на вашем локальном компьютере
- Убедитесь, что нет блокировки доступа к api.direct.yandex.com
- Проверьте, что не используется прокси

### Ошибка: "CORS policy"

**Причина:** Frontend не может обратиться к backend.

**Решение:**
- Backend уже настроен с CORS middleware (allow all origins)
- Проверьте, что backend запущен на порту 8000

### Данные не загружаются

**Причина:** Backend не запущен или недоступен.

**Решение:**
1. Убедитесь, что backend запущен: `python yandex_backend.py`
2. Проверьте консоль браузера на ошибки
3. Dashboard автоматически переключится на демо-данные

---

## 🌐 Деплой на Production

### Vercel (Рекомендуется для frontend)

Frontend уже настроен на Vercel. Для backend потребуется отдельный сервис.

### Backend деплой (варианты):

#### 1. Railway.app (Бесплатно):

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите и деплойте
railway login
railway init
railway up
```

#### 2. Render.com:

1. Создайте новый Web Service
2. Подключите репозиторий
3. Настройте:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python yandex_backend.py`
4. Добавьте переменные окружения из `.env`

#### 3. VPS (Digital Ocean, AWS, etc):

```bash
# На сервере
git clone your-repo
cd your-repo
pip install -r requirements.txt

# Запуск с Gunicorn
pip install gunicorn
gunicorn yandex_backend:app --workers 4 --bind 0.0.0.0:8000
```

### Обновление Frontend после деплоя backend:

В файле `demo/js/yandex-direct.js` замените URL:

```javascript
const API_BASE_URL = 'https://your-backend-url.com/api/yandex-direct';
```

---

## 📊 Примеры использования API

### Получение статистики (curl):

```bash
curl http://localhost:8000/api/yandex-direct/stats
```

**Ответ:**
```json
{
  "total_impressions": 245380,
  "total_clicks": 8752,
  "total_cost": 127450.5,
  "avg_ctr": 3.57,
  "avg_cpc": 14.56,
  "total_conversions": 284,
  "conversion_rate": 3.24,
  "campaigns_count": 5,
  "period": "2024-10-29 — 2024-11-28"
}
```

### Получение кампаний:

```bash
curl http://localhost:8000/api/yandex-direct/campaigns
```

### Экспорт в CSV:

```bash
curl http://localhost:8000/api/yandex-direct/export --output report.csv
```

---

## 🔐 Безопасность

### Важные замечания:

1. **Не коммитьте .env файл в Git!**
   - Файл `.env` добавлен в `.gitignore`
   - Токены должны храниться в переменных окружения

2. **Production deployment:**
   - Используйте HTTPS
   - Настройте CORS только для вашего домена
   - Регулярно обновляйте токены

3. **Переменные окружения:**
   - На production используйте секреты платформы
   - Не храните токены в коде

---

## ✅ Чеклист готовности

- [x] `.env` файл создан с токеном и логином
- [x] Backend сервер `yandex_backend.py` настроен
- [x] Зависимости установлены
- [x] Frontend обновлён для API интеграции
- [ ] Backend запущен локально
- [ ] Frontend подключён к backend
- [ ] Данные отображаются в Dashboard
- [ ] Backend задеплоен на production (опционально)

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи backend сервера
2. Откройте консоль браузера (F12)
3. Убедитесь, что токен действителен
4. Проверьте документацию: https://yandex.ru/dev/direct/doc/

---

## 🎯 Следующие шаги

### Улучшения для рассмотрения:

1. **Кэширование данных** - уменьшить количество запросов к API
2. **Автообновление** - добавить setInterval для обновления каждые 5 минут
3. **Детальная статистика по кампаниям** - отдельный endpoint для каждой кампании
4. **Управление кампаниями** - возможность старт/стоп через UI
5. **Интеграция с Яндекс.Метрикой** - дополнительная аналитика

---

**Статус:** ✅ API интеграция настроена и готова к запуску
**Дата:** 2025-11-28
**Ветка:** `claude/setup-api-integration-017JvQeHaHbsMMCH7ZBeeUC2`

🚀 Удачи с запуском!
